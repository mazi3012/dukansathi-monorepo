{
    "name": "dukansathi-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "dev": "cd frontend && npm run dev",
        "lint": "cd frontend && npm run lint",
        "pwa:build": "cd frontend && npm run build",
        "pwa:dev": "cd frontend && npm run dev"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}